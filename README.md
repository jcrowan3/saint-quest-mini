# Saint Quest — Mini MVP

A fast web prototype for Saint Quest.

## Run locally
```bash
pip install -r requirements.txt
streamlit run app.py
```

## Deploy
Use the same Cloud Run + Docker approach as Feast Day.
